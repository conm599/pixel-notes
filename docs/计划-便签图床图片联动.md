# 计划：便签 ↔ 图床图片联动（粘贴直传图床）

> 状态：**待站长拍板后实施** ｜ 编写：2026-09-06 ｜ 基于仓 HEAD `579b2a9` + 全量代码勘察
> 目标一句话：在便签（含 AI 对话框）粘贴图片时，首次征求用户同意《图床使用规范》，同意后图片自动上传到图床「便签」文件夹（无则自动创建），粘贴位置替换为可公开访问的图片直链；AI 对话框粘贴走同一逻辑。

---

## 1. 现状事实（代码勘察结论，计划的立足点）

| # | 事实 | 出处 |
|---|---|---|
| F1 | 便签与图床**共享同一个 PHP 会话**（父域 Cookie + 同机 session 池，账号合并 `ensure_pn_account` 自动建档） | 交接文档 15/16 轮、`tuchang/config.php` `is_logged_in()` |
| F2 | 图床 api.php **会话段已有 upload**：multipart 字段 `img`，魔数白名单 + GD 重编码 WebP60%（剥 EXIF）+ 配额检查 | `tuchang/api.php:656` |
| F3 | 会话段有统一 CSRF 门 `csrf_ok()`（api.php:417）；`csrf_token()` 惰性写入 session | `tuchang/api.php`、`tuchang/config.php:147` |
| F4 | `ensureApiFolder($uid)` 模式已存在（API Key 上传自动归档「api」夹） | `tuchang/api.php:33` |
| F5 | 便签 md.js **已支持渲染** `![alt](url)` 与 `<img>` → `.md-img` | `bianqian/js/md.js:105,171` |
| F6 | 便签 CSP `img-src 'self' https: data:` —— **生产 https 图链天然放行**；本地 http 测试需注入例外 | `bianqian/config/database.php:356` |
| F7 | 便签 app.js **目前没有任何 paste 处理器**（编辑器和 AI 对话框都是从零新增） | rg "paste" 零命中 |
| F8 | 图片公开直链形态 = `i.php?id=<id>&t=<share_token>`（token 即能力，撤销/过期回 notfound.webp 占位图）；`s.php?t=` 是 HTML 观看页 | `tuchang/i.php:11-43` |
| F9 | 分享 API：会话段 `share`（hours，0=永久）；分享机制属红线**一行不动**，只允许调用 | 交接红线 |
| F10 | 配置读取链：`PSU_* env > suite-config.php > 代码默认值`（两站同构）；图床文件夹表 img_folders 多层嵌套/环检测/同父重名拒绝均已上线 | `*/config*.php`、交接 9-3 深夜 |
| F11 | 便签侧 `siblingUrl('tuchang', path)` 已能动态算出图床完整 URL（第二域名/子目录自适应） | `bianqian/config/database.php:380` |

## 2. 用户流程（三个场景）

```
场景A 编辑器粘贴            场景B AI 对话框粘贴           场景C 二次及以后
Ctrl+V 图片 →               Ctrl+V 图片 →                Ctrl+V 图片 →
  ├ 未同意过？→ 弹《图床使用规范》卡片（同意/暂不/不再询问）
  │    同意 → 记 pn_users.img_consent → 继续
  │    暂不 → 本次跳过（不记忆），图片不插入，toast 说明
  │    不再询问（拒绝）→ 记拒绝，进设置可改
  ├ imgbridge.upload(file)
  │    取 CSRF（首次 GET tuchang api.php?action=csrf 播种）
  │    POST api.php?action=upload (img=file, folder_id=便签夹)
  │    POST api.php?action=share (id, hours=0)   ← 只调用不改分享机制
  │    组直链 https://<tuchang>/i.php?id=<id>&t=<token>
  ├ 成功 → 光标处插入 `![截图 2026-09-06](直链)` → md.js 渲染出图
  ├ 失败 → 降级：编辑器不插入 + toast 原因；AI 对话框保底走旧路径
  └ 全程并发窗口=1，同一粘贴去抖
```

## 3. 关键设计决策（每条都给了理由，可拍板改）

- **D1 链接形态用 `i.php?id=&t=` 直链，不用 s.php**：s.php 是 HTML 观看页没法嵌进 markdown；`i.php?id&t` 是原始图片流、token 即能力、可在图床随时撤销/改有效期，撤销后自动变 notfound 占位图（F8），便签侧零维护。
- **D2 上传走「浏览器直连图床 api.php 会话段」**：同 PHP 会话天然带登录态（F1），不用在便签服务器存图床 API Key，不经过便签服务器中转（省一跳、省内存、带宽不占两份）；与 dashboard.js 现行上传通道完全同构（F2）。
- **D3 目标文件夹 = 用户根级「便签」夹**：复制 `ensureApiFolder` 模式做 `ensureNotesFolder`，幂等（存在即返回）。用户在图床删了它也没事——下次粘贴自动重建。
- **D4 上传后自动 `action=share hours=0`（永久）**：只调用现有分享接口不改它（红线）。理由：没有分享 token 的图对非登录访客不可见（i.php 需 owner 会话），便签分享出去图片就瞎了。用户可在图床随时改时长/撤销。
- **D5 同意机制 = 版本化政策 + 三态记忆**：`pn_users` 加 `img_consent` / `img_consent_at` / `img_policy_ver` 三列（ensureTables 自愈 ALTER）。政策文本存 `pn_settings`（管理页可改）+ 版本号；版本升级后再次弹窗确认。弹窗三选：**同意并继续 / 暂不（本次跳过，不记忆）/ 不再询问（记忆拒绝）**；设置菜单加「🖼 图床图片」项可随时改主意。
- **D6 失败降级原则：粘贴链路永不因图床死而断**：上传失败/超时（8s+重试1次）/CSRF 拿不到 → 编辑器场景 toast 失败原因、不插入内容；AI 对话框场景保底插入原 blob 逻辑（若未来 AI 支持看图）或占位文本。图床不可达不影响便签其它功能。
- **D7 图片命名**：`便签-<yyyymmdd-HHMMSS>`，避免「image.png」满天飞，图床里可辨识。
- **D8 隐私语义写进规范文案**：**链接即能力**——谁拿到 i.php 链接谁就能看图；便签若公开分享，图也公开。规范里必须加粗写明 + 指出可在图床撤销。

## 4. 改动清单（分文件；JS/CSS 改动全部 bump ?v=）

### 图床侧（tuchang/，整文件可覆盖部署）
| 文件 | 改动 |
|---|---|
| `api.php` | ① 会话段 GET 白名单加 `action=csrf`（返回当前 `csrf_token()`，只读播种，响应头 no-store）；② 会话段 upload 支持 `folder_id` 参数 + `ensureNotesFolder($uid)`（照抄 ensureApiFolder，夹名「便签」）；③ upload 加 `rate_check('up'.$uid, 30, 600)` 十分钟 30 张限频 |
| 其余 | **零改动**——分享机制、i.php、s.php、view.php 一行不动（红线） |

### 便签侧（bianqian/）
| 文件 | 改动 |
|---|---|
| `js/imgbridge.js`（**新**） | 唯一的联动模块：`ImgBridge.init(ctx)`（依赖注入：tuchangUrl/csrf获取/同意状态查询/toast）；对外 `pick(file) → Promise<{url,name}>`；内部：同意检查→上传→分享→组直链；并发窗口1；8s 超时+1 次重试；全失败降级返回 null |
| `js/app.js` | ① 编辑器 textarea `paste` 监听（clipboardData.items 过滤 `image/*`，取文件走 ImgBridge，光标处插 markdown）；② AI 对话框输入框 `paste` 监听（同一 ImgBridge，插入同款 markdown，消息文本进 AI 流程）；③ 设置菜单加「🖼 图床图片」项（查看/修改同意状态、跳图床 dashboard） |
| `js/md.js` | 已支持图片渲染（F5）——仅确认 `.md-img` 深色主题下显示效果，预计**零改动或只加懒加载** |
| `css/pixel.css` | `.md-img` 像素风边框/最大宽度/圆角禁用；`?v=` bump |
| `index.php` | 引入 imgbridge.js + 上述资源 `?v=` bump；注入 `IMG_BRIDGE_CFG`（tuchangUrl=siblingUrl('tuchang','')、policyText、policyVer、consent 状态） |
| `api/auth.php` 或 notes.php | 无业务改动；`ensureTables` 加三列自愈 ALTER |
| `config/database.php` | 仅一处小改：CSP 行追加 `$cspImgLocal = getenv('PSU_LOCAL_IMG_HOST')` 非空时把该 host 并进 img-src（**生产无感**，本地测试用）；⚠ 部署走 VPS 原位改（红线：便签 database.php 禁整文件覆盖） |
| `share.php` | 确认 nonce CSP 的 img-src 同样放行 https 图（F6 同款），预计零改动 |
| `protocol.md` / `ai.php` / `ai-direct.js` | **不动**。论证：粘贴发生在消息发出**之前**，AI 收到的已是含图片直链的纯文本，服务端/直连两端收到相同文本，无两端逻辑分叉，不触碰「protocol.md 唯一事实源」铁律 |

## 5. 数据库变更（都有自愈路径）

- `pn_users`：`+ img_consent TINYINT(1) DEFAULT 0`、`+ img_consent_at INT UNSIGNED DEFAULT 0`、`+ img_policy_ver INT UNSIGNED DEFAULT 0`（ensureTables ALTER 自愈）
- 图床侧：**零表变更**（复用 img_folders/img_images/share_token，schema 已从 VPS dump 验证）

## 6. 安全与滥用防护

1. **CSRF**：上传/分享 POST 带会话段 csrf token；token 由新增只读 GET `action=csrf` 播种（no-store，不泄露多余信息）。
2. **限频**：上传 30 张/10 分钟（rate_check 文件守卫），防脚本刷配额。
3. **配额**：服务端 user_used/user_quota 硬校验已有（F2），前端 ImgBridge 把报错文案透传。
4. **内容安全**：GD 强制重编码 WebP 剥 EXIF 已有；魔数白名单已有；便签 CSP img-src https: 不放大攻击面（比现有 B 站/YouTube 嵌入宽得多——生产零改动）。
5. **隐私**：规范文案明示「链接即能力 + 公开便签=图公开 + 可随时在图床撤销」；同意时间与政策版本落库可审计。
6. **内容责任**：规范文案含「禁止违法内容，违规图床管理员可删」条款（图床本就有 adminws 管理与删除能力）。

## 7. 边界与漏洞清单（你担心的「漏洞与不完善」，逐条对策）

| # | 漏洞/边界 | 对策 |
|---|---|---|
| 1 | 便签公开分享 → 图公开泄露 | D8 文案明示；分享 token 可撤销；不默认公开便签 |
| 2 | 图床宕机/域名被墙 | D6 降级：编辑器不插入不断链；AI 保底；便签其它功能零影响 |
| 3 | 用户拒绝同意 | 不上传不插入；toast 引导设置里改；「不再询问」可逆 |
| 4 | 配额满/图床被删夹 | 报错文案透传；「便签」夹幂等重建（D3） |
| 5 | 非图片粘贴（文本/截图工具特殊格式） | 只拦 `image/*` 且 file 类型；文本粘贴走浏览器原生 |
| 6 | 同一张图反复粘贴 | v1 每次新传不去重（简单可靠）；图床夹内可删；P2 再做内容哈希去重 |
| 7 | 图被删/分享过期撤销 | i.php 回 notfound.webp 占位（已有），便签显示破图占位可接受 |
| 8 | 第二域名/子目录部署 | 直链域名用 `siblingUrl('tuchang')` 动态前缀（F11），PREFERRED_HOST 红线不碰 |
| 9 | 弱网/超时 | 8s 超时 + 1 次重试 + 失败降级；并发窗口 1 防连点爆量 |
| 10 | AI 直连模式（不过服务器） | 上传是浏览器侧行为，与 AI 通道无关，两模式天然一致 |
| 11 | 移动端 | paste 事件 + 长按粘贴同源触发；`md-img` 移动端样式随 pixel.css 断点走 |
| 12 | Ctrl+Z | 插入的是纯文本，编辑器原生撤销可用 |
| 13 | 便签注销/账号删除 | 图床侧 ensure_pn_account 映射的图不自动删——P2 再议（联动删除是独立风险项，先不做） |
| 14 | 时间线：粘贴→上传中用户就点保存 | 插入发生在上传成功后（异步等待期间光标占位符 `⏳上传中…`，完成后替换），保存拿到的永远是最终 URL |

## 8. 本地测试环境（已就位 ✅）

- PHP 8.3.33（C:\dev\php）+ MySQL 8.0.28（服务 MySQL8，手动启动）+ Git + Python
- 双库已建：`pixel_notes`（便签，user pixel/pixel_local）、`img_local`（图床，user imglocal，4 张 img_* 表 = VPS 生产 schema 1:1 dump）
- 跑法：`php -S localhost:8080 -t bianqian`、`php -S localhost:8081 -t tuchang`，启动注入 `PSU_BIANQIAN_DB_*` / `PSU_TUCHANG_DB_*` / `PSU_LOCAL_IMG_HOST=http://localhost:8081`
- 会话桥：同 localhost 不同端口 Cookie 天然互通（Cookie 不分端口），两台 php -S 共用 C:\dev\php\sessions → 账号合并链路本地可复现
- 测试账号：tester/test123456（本地注册流需 SMTP，直接 SQL 插 bcrypt 行 + is_admin=1）
- 注意：本地 http 下 CSP 需 `PSU_LOCAL_IMG_HOST` 注入（生产 https: 免）

## 9. 测试与 QA 计划

**自动化**（仿 `_test_match.js`/`_test_sse.js` 本地文件惯例，不入库上线）：
- `_test_imgbridge.js`：直链组装、降级路径、并发去抖、超时重试逻辑（mock fetch）
- `_test_md_img.js`：md.js 对含图 markdown 的渲染快照（`![alt](i.php?id=&t=)`、HTML img、恶意 onerror 过滤复测）

**手动 QA 清单（15 条，全过才算完）**：
1. 首次粘贴弹规范卡 → 同意 → 图落「便签」夹 + 直链入文 + 渲染出图
2. 二次粘贴不再询问；设置里可看/改同意状态
3. 「暂不」本次不插入不记忆，下次再问；「不再询问」记忆拒绝
4. AI 对话框粘贴：同逻辑，AI 收到的文本含直链（抓请求验证）
5. 图床停服（本地停 php -S）→ 编辑器/AI 双降级，无崩溃无死链插入
6. 配额打满 → 文案准确；删几张图恢复后可继续
7. 图床删「便签」夹 → 下次粘贴自动重建且历史链接不受影响
8. 撤销分享 → 便签里变占位图；恢复分享 → 图回来
9. 公开分享带图便签 → 访客（无痕窗口）能看到图
10. 移动端模拟（触屏 UA + 窄视口）粘贴与渲染
11. 控制台零 CSP 报错（双站）
12. 大图（>10MB）→ 明确报错不崩；GIF/WEBP/PNG/JPG 四格式各一
13. 便签搜索/备份/朗读不受含图内容影响
14. 幂等：「便签」夹重复 ensure 不产生同名夹
15. 回归：便签原有 AI 编辑/整理/文件夹功能全绿（跑既有 _test_match/_test_sse）

**真实用户体验走查**：站长真机（PC + 手机）各过一遍场景 A/B，重点感受弹窗文案与插入速度。

## 10. 实施顺序（每步可独立验收）

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| T0 | 本地环境收尾（tester 账号、双站起服务、会话桥验证） | 本地登录便签 → 直开图床 dashboard 免登 |
| T1 | 图床侧：csrf 端点 + ensureNotesFolder + upload folder_id + 限频 | curl 会话流 4 连测全绿 |
| T2 | 便签侧：imgbridge.js + 编辑器粘贴链路 | 场景 A 全流程本地跑通 |
| T3 | AI 对话框粘贴 | 场景 B 全流程 + 抓包验证 |
| T4 | 样式/CSP/设置项/share.php 复核 | QA 清单 1-4、11 |
| T5 | QA 全量 + 自动化测试 + 修复 | 15 条全过 |
| T6 | 部署预案（VPS）+ 站长真机走查 | 生产验收（含 database.php 原位改 CSP 如需） |

## 11. 待站长拍板（我已给默认值，不改就按默认做）

- **Q1 默认永久有效期**：✅ 默认 0=永久、图床可改（备选：7 天自动续期，复杂不建议）
- **Q2 拒绝记忆**：✅ 提供「不再询问」勾选（可逆，设置里能改回）
- **Q3 直链域名**：✅ 主域名（tuchang.naxid.top / 动态第二域名），PREFERRED_HOST 优选域不动（红线）
- **Q4 图床里「便签」夹的用户操作权**：✅ 允许改名删除（删了自动重建，历史直链不受影响）
- **Q5 规范文案**：草稿在实现时给出，要点=存储位置/配额、链接即能力、公开便签=图公开、可撤销、违法内容条款、政策版本升级会再次询问

---

*批准后按 T0→T6 顺序开工，每阶段提交一次本地仓（push 仅本地，云端推送等窗口+凭证）。*
